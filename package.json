{
  "name": "ping_pong_elo_calc",
  "version": "1.0.0",
  "description": "Calculate ping pong elos",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "npx nodemon",
    "start": "npx tsc --outDir ./build src/index.ts && node build/index.js"
  },
  "dependencies": {
    "mathjs": "^11.8.2",
    "readline": "^1.3.0"
  },
  "author": "Hayden Howle",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.4.1",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
